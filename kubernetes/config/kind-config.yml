kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
nodes:
- role: control-plane
- role: worker
- role: worker
networking:
  # This allows the API server to be reached via the Docker bridge
  apiServerAddress: "0.0.0.0" 
  apiServerPort: 6443
